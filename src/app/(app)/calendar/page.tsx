import { ComingSoon } from "@/components/app-shell/coming-soon";

export const metadata = { title: "Calendar · Bot OS" };

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      blurb="Plan your week of posting across pillars and funnel stages. Drag, drop, schedule."
    />
  );
}
