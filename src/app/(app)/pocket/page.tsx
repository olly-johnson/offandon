import { ComingSoon } from "@/components/app-shell/coming-soon";

export const metadata = { title: "Off&On Pocket · Bot OS" };

export default function PocketPage() {
  return (
    <ComingSoon
      title="Off&On Pocket"
      blurb="Save ideas, hooks, and references on the fly. Pull them into the Script Writer with one click."
    />
  );
}
