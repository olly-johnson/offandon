import { ComingSoon } from "@/components/app-shell/coming-soon";

export const metadata = { title: "Performance · Bot OS" };

export default function PerformancePage() {
  return (
    <ComingSoon
      title="Performance"
      blurb="Cross-reference Format, Funnel stage, and Pillar to find what actually drives DMs and calls. Powered by your Instagram metrics once connected."
    />
  );
}
